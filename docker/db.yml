version: '3.3'

services:

  db:
    image: postgres
    environment:
      - "POSTGRES_PASSWORD=root"
      - "POSTGRES_USER=root"
      - "POSTGRES_DB=spotify_matcher"
    ports:
      - "5432:5432"